function About({ id }) {
    return (
        <section id={id} className="min-h-screen flex items-center justify-center bg-slate-900 border-b border-slate-800">
            <h1 className="text-4xl font-bold text-teal-400">About Me Section</h1>
        </section>
    );
}

export default About;